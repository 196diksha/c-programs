#include<stdio.h>
void main()
{
int a[15],i,*p,n;
p=a;
printf("Enter no. of elements u want to enter:");
scanf("%d",&n);
printf("Enter array elements:");
for(i=0;i<=n;i++)
{
scanf("%d",p+i);
}
for(i=0;i<=n;i++)
{
printf("%d",*(p+i));
}
getch();
}